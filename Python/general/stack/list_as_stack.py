# init stack
stack: list = []

# add values to the stack
stack.append("eat")
stack.append("sleep")
stack.append("code")

print(f"Stack values: {stack}")

# pull the last value from the stack
last_value = stack.pop()

print(f"The last value from the stack: {last_value}")

print(f"Stack values: {stack}")

# pull the last value from the stack
last_value = stack.pop()

print(f"Stack values: {stack}")
